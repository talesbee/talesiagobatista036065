import { HeaderPage } from '../components';

export default function Tutors() {
  return (
    <div className="flex-1 p-4 pt-4">
      <HeaderPage searchInput="" setSearchInput={() => {}} onSearch={() => {}} />
    </div>
  );
}
